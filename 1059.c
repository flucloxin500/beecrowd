#include <stdio.h>
#include <math.h>

int main (){

    int x = 2, n;

    while (x <= 100)
    {
        if ( x % 2 == 0)
        printf("%d\n",x);
        x+=1;
    }
    

    

return 0;
}